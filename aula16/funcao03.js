let v = function(x) { // em JS podemos jogar uma função dentro de uma variável (por ser uma linguagem funcional)
    return x*2
}

console.log(v(5))

// eu passei para a minha variável um parâmetro e ela executou uma ação